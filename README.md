# Landing under disturbance — LQR vs learned control

Two controllers. One rocket. Find out which holds when the wind picks up.

See [`docs/overview.md`](docs/overview.md).

## Commands

```bash
uvx uvtask lint        # ruff check
uvx uvtask fmt         # ruff format
uvx uvtask check       # lint + fmt
uvx uvtask test        # pytest
uvx uvtask all         # lint + fmt + test
```
